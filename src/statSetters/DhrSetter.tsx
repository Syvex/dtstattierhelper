import NumberComparison from './CompareNumber.tsx';
import dhr from '../statArrays/dhr';

const DhrSetter = () => {
  return <NumberComparison title={'Direct Hit'} data={dhr} />;
};

export default DhrSetter;
