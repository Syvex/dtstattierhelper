import NumberComparison from './CompareNumber.tsx';
import det from '../statArrays/det';

const DetSetter = () => {
  return <NumberComparison title={'Determination'} data={det} />;
};

export default DetSetter;
