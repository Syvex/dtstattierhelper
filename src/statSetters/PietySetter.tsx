import NumberComparison from './CompareNumber.tsx';
import piety from '../statArrays/crit.ts';

const PietySetter = () => {
  return <NumberComparison title={'Piety'} data={piety} />;
};

export default PietySetter;
