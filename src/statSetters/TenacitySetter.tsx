import NumberComparison from './CompareNumber.tsx';
import ten from '../statArrays/ten';

const TenacitySetter = () => {
  return <NumberComparison title={'Tenacity'} data={ten} />;
};

export default TenacitySetter;
